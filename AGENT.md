# AGENT.md

이 문서는 **AI 에이전트(Claude 등) 또는 새로 합류한 개발자**가 이 저장소에서 작업할 때 먼저 읽어야 하는 가이드입니다.

## 1. 프로젝트 한 줄 요약

Opiki(오피키) — AI가 청년 혜택을 맞춤 추천하고 신청 일정을 관리해주는 웹앱. 현재는 **프론트엔드 + 목업 데이터** 단계이며, 실제 백엔드/AI API는 아직 연동되어 있지 않습니다.

## 2. 작업 전에 반드시 읽을 것

1. `README.md` — 기술 스택, 실행 방법, 폴더 구조
2. `docs/planning/` — PRD, IA/화면정의서, ERD, API 명세, 아키텍처, 비기능요구사항
3. `docs/handover/` — **가장 최근 날짜의 파일부터 읽으세요.** 직전 작업자가 무엇을 했고, 무엇이 아직 안 됐고, 다음 작업자가 주의할 점이 정리되어 있습니다.
4. Figma 디자인 (링크는 작업 히스토리/대화 기록 참고) — 이 저장소의 UI는 Figma를 기준으로 구현합니다. 디자인이 명확하지 않은 화면은 목업 데이터/기존 화면 패턴을 참고해 유사하게 구현하고, `docs/handover`에 "디자인 미확인, 추정 구현" 여부를 반드시 남기세요.

## 3. 작업 방식 (중요)

- **단계별(Step) 작업**: 한 번에 전체를 구현하지 않고, 화면/기능 단위로 나눠서 진행합니다.
- **매 작업 완료 시 아래 3가지를 함께 커밋·푸시합니다.**
  1. 실제 코드 변경
  2. `docs/handover/YYYY-MM-DD_stepN_작업명.md` — 작업 요약 + 다음 작업자에게 인수인계할 내용 (형식은 기존 handover 파일 참고)
  3. 필요 시 `README.md` 갱신
- **범위를 벗어나는 기능을 임의로 추가하지 않습니다.** 특히 "소비기록 기반 카드 추천" 기능은 기획 논의 끝에 **완전히 제외**되었습니다 (구현 불가/규제 이슈 — `docs/planning/01_PRD.md` 참고). 되살리지 마세요.
- **백엔드 연동은 아직 하지 않습니다.** 사용자가 명시적으로 요청하기 전까지는 목업 데이터(`src/mocks/`)로만 화면을 구성하세요.

## 4. 기술 스택 & 컨벤션

| 항목 | 내용 |
|---|---|
| 프레임워크 | React 19 + TypeScript + Vite |
| 스타일 | Tailwind CSS v4 (`@theme` 기반, `tailwind.config.js` 없음 — `src/index.css` 참고) |
| 상태관리 | Zustand (`src/store/`) |
| 라우팅 | react-router-dom (`src/App.tsx`) |
| 아이콘 | lucide-react |
| 배포 목표 | 웹앱 우선, 추후 Android 패키징 예정 (Capacitor 등 검토 — 아직 미착수) |

### 디자인 토큰 (`src/index.css`의 `@theme`)

| 변수 | 값 | 용도 |
|---|---|---|
| `--color-primary` | `#5b8cff` | 브랜드 블루 |
| `--color-mint` | `#6ee2c8` | 민트 (상시뱃지/그라데이션) |
| `--color-accent` | `#ff9f5a` | 오렌지 (마감임박 D-day) |
| `--color-navy` | `#1f2a44` | 본문/타이틀 텍스트 |
| `--color-muted` | `#a8b1c7` | 보조 텍스트/아이콘 |
| `--color-border` | `#dde3f3` | 구분선/보더 |
| `--color-bg` | `#f7f9fc` | 배경 |

새 색상이 필요하면 반드시 여기에 추가하고, 컴포넌트에서 임의의 hex 값을 하드코딩하지 마세요.

### 폴더 구조

```
src/
├── components/
│   ├── layout/     # Header, BottomNav, AppLayout (공통 레이아웃)
│   └── ui/         # BenefitCard, StatusBadge 등 재사용 컴포넌트
├── pages/          # 화면 단위 (라우트 1:1 매핑)
├── mocks/          # 목업 데이터
├── store/          # zustand 전역 상태
└── types/          # 공용 타입
```

- 새 화면 추가 시 `pages/`에 컴포넌트를 만들고 `App.tsx`의 라우트에 등록하세요.
- 화면 간 공유되는 UI는 `components/ui/`에 분리하세요 (예: 뱃지, 카드, 모달).
- 모든 화면은 모바일 기준(`max-width: 480px`, `.app-shell` 클래스)으로 작업하세요. 데스크톱 대응은 범위 밖입니다.

## 5. 커밋 전 체크리스트

```bash
npx tsc -b          # 타입 에러 없는지 확인
npm run build        # 빌드 통과 확인
```

두 명령이 모두 통과해야 커밋합니다.

## 6. 현재 진행 상황 확인

가장 빠른 방법: `docs/handover/` 폴더에서 **파일명 날짜가 가장 최신인 문서**를 여세요. 거기에 "아직 안 한 일" 표가 항상 포함되어 있습니다.
